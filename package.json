{
  "name": "codex-ai-agent",
  "version": "0.1.2",
  "description": "Run Codex CLI as a local AI agent with note context, approvals, and reviewable file changes.",
  "main": "main.js",
  "scripts": {
    "build": "node esbuild.config.mjs production",
    "dev": "node esbuild.config.mjs",
    "typecheck": "tsc --noEmit --skipLibCheck",
    "check": "node --check main.js",
    "release:check": "npm run typecheck && npm run build && npm run check"
  },
  "keywords": [
    "obsidian",
    "codex",
    "agent",
    "plugin"
  ],
  "author": "j4charlie",
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.25.0",
    "obsidian": "^1.5.12",
    "typescript": "^5.4.0"
  }
}
